# 2026-05-21

## Friends screen improvements

**Streak fix** (`src/lib/stats.js`)
- `buildPublicProfile` now filters sessions to `type === 'climb'` before computing the weekly streak — gym and hangboard sessions no longer count toward the streak.

**Friend detail view toggle** (`src/components/friends/FriendsScreen.jsx`)
- Added All Time / Last 90 Days toggle to `DetailView` (same pill style as ClimbingStats in Plan tab).
- Level cards now switch between `boulderLevel`/`ropeLevel` (all-time) and `boulderCurrent`/`ropeCurrent` (90d) based on the toggle.
- Cards now also show Proj / Flash sub-labels (matching the leaderboard row style) instead of the previous "90d: level" footnote.
