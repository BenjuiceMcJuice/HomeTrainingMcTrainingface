# 2026-08-19

## Schedule reminders — step 1: a reminder time per schedule entry

Picking up `docs/specs/betalog_reminders_spec.md`. Both reminder routes (calendar feed, web push)
were blocked on the same gap: `ScheduleEntry` had days but no time of day and no timezone. That's now
closed.

### The decision that shaped it

The spec left open whether reminders should be **one per entry** or a **daily digest**. Ben: one per
timed thing, so a morning and an evening reminder are both possible. So each entry carries its own
`remindAt` — hangboard 07:00 Mon/Wed/Fri and gym 18:30 Tue/Thu is two entries and two alerts, no
digest, no bundling. Both routes now build on that shape: one `VEVENT` per timed entry for Route A,
one push per timed entry for Route B.

### Files changed

- **`src/lib/types.js`** — `ScheduleEntry` gains optional `remindAt` ("HH:MM" local, 24h) and `tz`
  (IANA). Both optional, so existing schedules keep working untouched.
- **`src/lib/reminders.js`** *(new)* — `isValidRemindAt`, `localTimeZone`, `withReminder`,
  `sortByRemindAt`. Pure, no React imports, so `storage.js` and later the Worker can both use it.
- **`src/lib/__tests__/reminders.test.js`** *(new)* — 14 tests. All pass.
- **`src/hooks/useSchedule.js`** — `setReminder(id, remindAt)`, and `addEntry` takes an optional time.
- **`src/components/routines/ScheduleCard.jsx`** — bell + `<input type="time">` per entry with a
  Clear control, and an optional time on the add form.
- **`src/components/dashboard/ScheduleNotice.jsx`** — shows the time against each routine and orders
  due-today entries by it, untimed last.

### Decisions worth recording

- **Clearing deletes the keys, it doesn't null them.** `withReminder(entry, '')` removes `remindAt`
  and `tz` entirely. The spec says "absent = no reminder", and the `.ics` builder will test for
  absence — a stored `null` would have been a trap later.
- **No separate "Remind me" switch.** An empty time *is* the off state. One control, not two, and
  reminders stay off until a time is deliberately set.
- **`tz` is captured on every set**, not once globally, so moving timezone and re-saving fixes itself.
  Without it a Worker running in UTC is an hour out for half the year.

### Verified

`npm run build` clean. `npm run lint` 0 errors (16 pre-existing warnings). The 14 new tests pass; the
4 known MET/calorie failures in `stats.test.js` are untouched and still open.

Driven in a real browser (Chromium/Playwright, temporary local auth bypass, reverted): set 07:00 on
one entry and 18:30 on another — both persisted independently with `tz`; Clear removed `remindAt` and
`tz` from the second while leaving the first intact; the Dashboard notice rendered
"Due today: Max Hangs · 07:00 / 7:3 Repeaters · 18:30", sorted morning-first even though the evening
entry was stored first.

### Known limits (pre-existing, not introduced here)

The schedule caps at 3 entries and `ScheduleCard` won't schedule the same routine twice — so "the
same routine, morning and evening" isn't expressible. Two different routines at two times is. Small
change to lift either if it matters.

### Next

Route A — a Cloudflare Worker serving a private `.ics` at a token URL, `days[]` → `RRULE`, token in
KV, "Subscribe to calendar" in Settings. Nothing sends a reminder yet; this step only made it
possible to know *when*.

## Route A decisions settled (design discussion, no app code changed)

Talked through how the calendar feed behaves in practice. Recorded in the spec:

- **Only timed entries get a `VALARM`.** An untimed entry becomes a silent all-day event — visible,
  never buzzes. Corrects an earlier suggestion of a 09:00 alarm for untimed entries: clearing a time
  is how a user says "stop reminding me", so it must not produce a different nag.
- **`remindFrom` anchors `DTSTART` once**, stored when the reminder is set, so the feed is stable
  between refreshes instead of shifting on every request.
- **Stable `UID` per entry** so edits update the event and deletions remove it.
- **KV mirror on save** is the step that makes any change reach the feed — the Worker never reads
  Firestore.
- **One feed URL per user**, shared across devices; revoking the token kills all subscriptions.
- **Refresh lag accepted.** iOS decides when to refetch; can be hours. The schedule is a weekly
  pattern that rarely changes, so eventual correctness is fine. A deletion shortly before a reminder
  may still fire once.
- **Route B deferred.** Not a now problem.

Also confirmed as fine, not worth changing: one entry per routine, 3 entries max.

## Route A built — the calendar feed

End to end: set a time in Plan → Settings → "Set up calendar reminders" → subscribe once on the
phone → the Calendar app fires the alerts from then on.

### Architecture decision: the app renders the .ics, the Worker just stores it

The spec had the Worker building the calendar. Rendering it client-side instead means the iCalendar
logic lives in `src/lib/ics.js` where it's unit tested (22 tests), and the Worker is ~90 lines of
store-and-serve. One implementation, not two.

### Files

- **`src/lib/ics.js`** *(new)* — pure `.ics` renderer. RFC 5545 escaping and 75-octet line folding.
- **`src/lib/calendarFeed.js`** *(new)* — token generation, feed URLs (https + webcal), upload,
  revoke, and a cheap body fingerprint.
- **`src/hooks/useCalendarFeed.js`** *(new)* — enable / disable / sync.
- **`src/components/CalendarFeedSync.jsx`** *(new)* — mounted at the app root.
- **`src/lib/__tests__/ics.test.js`** *(new)* — 22 tests.
- **`src/lib/reminders.js`** — gained `firstMatchingDay`; `withReminder` now stamps `remindFrom`.
- **`src/lib/storage.js`**, **`types.js`** — `il_calendarFeed` / `CalendarFeed`, in `SYNC_KEYS`.
- **`src/components/layout/SettingsSheet.jsx`** — `CalendarReminders` section.
- **`workers/betalog-calendar/`** *(new)* — Worker, `wrangler.toml`, README.

### Decisions

- **Floating times, no `TZID`.** Deviates from the spec sketch, deliberately. A `TZID` reference
  obliges a correct `VTIMEZONE` per IANA zone including DST rules — hard to generate, easy to get
  subtly wrong. Floating means "07:00 wherever you are", which is what you want from a training
  reminder anyway. `tz` is still stored for Route B.
- **`CalendarFeedSync` mounts at the app root**, not in Settings — the schedule is edited in Plan, so
  a mirror scoped to Settings would miss every real edit.
- **In-flight guard on uploads.** First run pushed the same body three times: StrictMode double-fires
  the effect and the post-load `setData` changes the schedule array's identity, all before
  `pushedHash` lands in state. A module-level in-flight map collapses those. Now exactly one upload
  per real change.
- **`TRIGGER;RELATED=START:PT0S`** — the alarm fires *at* the time chosen. The spec's `-PT10M` would
  have surprised anyone who set 07:00 and got 06:50.
- **The token authorises writes as well as reads.** Symmetric with the read secret, and the
  alternative (authenticating a Worker to Firebase) is disproportionate for routine names and times.
  Documented in the Worker README.

### Verified

Build clean, lint 0 errors, 36 reminder/ics tests pass (the 4 MET/calorie failures in
`stats.test.js` are untouched and still open).

Driven in Chromium against a stand-in Worker with the same routes (temporary auth bypass, reverted):

1. Enabling generates a token, uploads the feed, and stores the record.
2. The feed is correct — timed entry with a `VALARM` at 07:00, untimed entry as a silent all-day
   event, one `VEVENT` each, stable UIDs.
3. Editing a time **in Plan** reaches the feed.
4. Clearing a time leaves the event, drops the alarm.
5. Deleting an entry removes its event.
6. An unchanged schedule uploads nothing.
7. Revoking returns 404 on the URL and clears the local record.

### Not done

**The Worker isn't deployed.** Needs `wrangler kv namespace create FEEDS`, the id in
`wrangler.toml`, then `wrangler deploy`. Until then the Settings button will fail — the app defaults
to `https://betalog-calendar.benjuicemcjuice.workers.dev`, overridable with `VITE_CALENDAR_API`.
Nothing was tested against real Cloudflare KV or a real phone subscription.

## Deploy instructions recorded, merged to main

The `wrangler` steps are in `DEVLOG.md` under "TODO on the laptop — deploy the calendar Worker", to
be run from the laptop (wrangler needs a browser login and the Cloudflare account).

Merged to `main` on Ben's explicit instruction. Two deviations from CLAUDE.md worth recording:

- **Straight to `main`, no `preprod`.** The branching model routes work through `preprod` first;
  that branch doesn't exist in the repo yet. Merged the feature branch directly, fast-forward from
  the commit it was based on.
- **`npm test` is not green.** The 4 pre-existing MET/calorie failures in `stats.test.js` are still
  open and CLAUDE.md treats them as blocking promotion. They are unrelated to this work and predate
  it; `npm run build` and `npm run lint` are clean and the 36 reminder/ics tests pass.

Since `main` auto-deploys to betalog.co.uk, this is live. Until the Worker is deployed, tapping
"Set up calendar reminders" shows an error message — it fails gracefully rather than breaking
anything, and the rest of the app is unaffected.
