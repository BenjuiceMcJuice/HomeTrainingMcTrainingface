# 2026-05-28

## Goals feature — full implementation

Built the goals feature from the spec in `docs/specs/betalog_goals_spec.md`.

### Files created
- `betalog-react/src/hooks/useGoals.js` — hook + pure helper exports (`getCurrentValue`, `calcGoalProgress`). Auto-achieves goals via `useEffect` when sessions/weight log changes.
- `betalog-react/src/components/goals/GoalsSection.jsx` — Plan > Profile tab section. Active goal cards with progress bar (start→target), days remaining (amber ≤30d, red ≤7d), edit/delete. Achieved goals in compact green rows. Add/Edit slide-up sheet with type picker (locked on edit), grade chip grid for climbing, numeric input for cardio/weight, date picker.

### Files modified
- `src/lib/storage.js` — added `il_goals` key to `load()`, `saveGoals()`, `SYNC_KEYS`, `mergeFromCloud`
- `src/lib/types.js` — added `Goal` typedef, added `goals` to `BetaLogData`
- `src/components/profile/ProfileTab.jsx` — imported and rendered `GoalsSection` between profile card and ClimbingStats
- `src/pages/Dashboard.jsx` — added `GoalsWidget` (auto-shown when active goals exist, taps to Plan), added `useGoals()` call for auto-achieve on load, updated `CoachTip` to pass goals/weightLog to `buildContext`
- `src/pages/Coach.jsx` — updated `buildContext(sessions, profile, goals, weightLog)` to inject active goals into Groq context (type, current value, target, deadline, % progress)

### Build
Clean — no errors.
