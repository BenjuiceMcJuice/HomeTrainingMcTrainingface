# 2026-09-18

Ben, with a screenshot of the Dashboard: *"I've noticed that sometimes the bottom tabs float
around the place."* The tab bar was sitting halfway up the page, with a white smear underneath
it where the blur had been painted at the bar's old position.

---

## BTL-B44 — bottom tabs floating

**Merged 2026-09-18 on Ben's word — *"Push to main."* Not seen on a phone first; the check below still applies to the live site.**

**Cause.** The mobile bottom nav is `position: fixed` with `backdrop-filter: blur(12px)`.
WebKit has a long-standing compositing bug where a fixed element carrying a backdrop blur is
painted at a stale scroll offset after the document's height changes underneath it — a widget
collapsing or expanding, a sheet closing, the keyboard dismissing. The page scrolls, the bar
does not follow, and the blur layer smears the content it used to sit over. That is exactly the
screenshot: bar mid-page, whited-out band beneath it, "V4 / V5" rows showing through the edge.

**Fix — `src/components/layout/Nav.jsx`.**
- `backdropFilter` removed from the bottom nav. Background is now solid `#ffffff` instead of
  92% white; there was almost nothing to see through it anyway.
- `transform: translateZ(0)` on the bar so it has its own compositing layer and is repositioned
  with the viewport rather than repainted with the page. A transform on the fixed element itself
  does not break its fixed positioning — only a transform on an ancestor does.
- The sticky top header and the desktop nav keep their blur — the bug is specific to `fixed`.
- Comment in the JSX says why, so nobody adds the blur back for looks.
- Cache v33 → v34 so installed phones drop the old bundle.

**668 tests, 0 lint errors, build clean.** The bug is intermittent and only reproduces on iOS, so
it is not verified on a phone. Check on the preview deploy: open the Dashboard, expand and collapse
the Boulder widget a few times mid-scroll, open and close a log sheet, and the bar should stay on
the bottom edge throughout.

Released the same day on Ben's instruction.

---

## Friends see the pyramid

Ben, on the friends page: *"Can we review what we share with friends. Perhaps the climbing
pyramid as a general view and/or the chart also with an all time / last 30 days perhaps or just
last 30 days maybe? And owned / best grade."* Then: *"Happy to go with your recommendations for
now. It's climbing focused only."*

**Merged 2026-09-18 on Ben's word — *"Push to prod."* — after four passes on the branch
`claude/friends-info-sharing-review-rrfmpf`, described below. Not seen on a phone first; BTL-B45
is the check on the live site.**

**What was wrong first.** The friend detail view had an *All Time / Last 90 Days* toggle. Since
Q3 (2026-09-13) the *All Time* tab showed Base, Best and Flash overlaid from the 180-day
pyramid, so it was not all time. The *Last 90 Days* tab read `boulderCurrent` / `ropeCurrent`,
the older 90-day stats that were never given a base, so `ownedGrade` fell back to the retired
consistent grade and the big number changed meaning between the two tabs. The data-honesty spec
still listed this surface as its weakest, and it was right.

**Decided.** One window, the pyramid's 180 days — not 30 (a base needs eight credited sends at
one grade; a month rarely holds that, and it would be a third window the app reads nowhere else)
and not all time (a base is a statement about the window). Owned and best were already shared;
what was missing was the pyramid behind them and the basis.

**Built.**
- `lib/goals.js` — `impliedGradeTarget(type, reading)`: the next rung above the base, or above
  the project with no base. Lifted out of `Dashboard.jsx`'s `readPyramid` so the profile and
  the Dashboard cannot pick different targets. `buildPublicProfileWithBase` now adds to each
  level: `pyramid` (`target`, `tiers` as `{grade, need, have, met}`, `label`, `sessionCount`,
  `windowDays`, `basis`), `grades` (`{attempts, sends, flashes}` per grade over the window) and
  `allTimeBest` (the `stats.js` all-time hardest send, read before the window's `project`
  overwrites the key). Older keys stay.
- `pages/Dashboard.jsx` — calls the helper; `V_GRADES` / `FRENCH_GRADES` imports dropped.
- `components/friends/FriendsScreen.jsx` — the toggle is gone. One `DisciplineCard` per
  discipline: level badge, the grade owned, Best / Flash / All-time (all-time only when it
  differs from the window's best), the basis line top right, then `PyramidChart` (compact,
  "Base for V5 · next up") and `GradeChart` + `Legend`, the Dashboard's own components. A
  friend whose profile has no `pyramid` key reads *Pyramid not shared yet — their app needs to
  sync on the latest version*. Recent sessions unchanged.
- Tests: the published tiers and their shape, per-grade counts, all-time best surviving the
  overlay, an empty window publishing an empty pyramid with its basis, and the target helper.
- Cache v34 → v35.
- Specs: the pyramid spec's §6 *Friends: not done* line and decision-table row 4 were both stale
  since 2026-09-13 — fixed. The honesty spec's §6 row for friends updated.

**Left open.** BTL-B31 grows: the privacy copy must list what friends now see, including
per-grade attempt counts, which is new disclosure. BTL-B45: check on a phone.

**675 tests, 0 lint errors (13 warnings, all pre-existing), build clean.** Not seen running —
the friends page sits behind sign-in and a second account.

### Second pass — two boards, and every window said

Ben: *"It needs to be clear exactly what data we're showing. I'd like it to be to help stoke
competition between friends on what they're climbing. With the leaderboard aspect also somehow."*
Asked two questions, both answered with the recommendation: the competitive board ranks on the
hardest send then send count, over a rolling 30 days.

**Why a second board.** The existing board ranks on the level word, which is the Base over 180
days. Fair, and slow — eight sends at one grade — so nothing on it moves when a friend has a big
session. Competition needs a number that moves. A base is a claim about what is built and needs
the long window; hardest send and send count are plain counts, which the honesty spec allows at
any window it names.

**Built.**
- `lib/goals.js` — `RECENT_WINDOW_DAYS = 30`. Each level on the profile adds `recent`
  (`windowDays`, `hardestSend`, `sends`, `flashes`, `sessions`, from `buildPyramid` over 30 days)
  and `lastClimbedAt` (last session date with a climb in the discipline).
- `FriendsScreen.jsx` — **Level** and **Last 30 days** tabs under the discipline switch, with a
  sentence under them saying exactly what the board ranks on, and *How this works ↗* to the
  explainer. `rankEntries(entries, discipline, board)`: level → LEVEL_RANK, Base, then Best;
  recent → hardest send, sends, sessions; profiles without `recent` sort last, shown as *not
  shared yet*. Each row says when they last climbed, green within 7 days (`ACTIVE_DAYS`), grey
  after. The right-hand block answers only the current board's question. *–* for no level is
  now *no base yet*.
- Detail card, in window order: **Last 30 days** (hardest send · sends · flashes · sessions),
  **Last 180 days** (level, Base "the grade owned, 8 sends", Best, Flash, the pyramid, then
  *Per grade, last 180 days* bars), **All time** (hardest send). All-time no longer hides beside
  Best when they differ; it has its own labelled line.
- Tests: the 30-day counts and last-climb date, per discipline, and the empty window.
- Rendered both boards and the card server-side with four fake friends (new build, old build,
  quiet, hot) to check the order and the copy; throwaway harness removed.

**677 tests, 0 lint errors, build clean.** Still on the branch; still not seen on a phone.

### Third pass — older logs publish on first open

Ben: *"Will older data sync with this update?"* The log, yes — the profile is built from the
whole session log already on the device, so history is in the pyramid the moment it is
published. The publishing was the gap: the profile document is written only from
`syncToFirestore`, which runs after a `setData` or on a first-ever sign-in. A friend who
updated the app and then rested would keep the old document up and read as *not shared yet*
until their next logged session.

**Built.** `PUBLIC_PROFILE_VERSION = 2` in `lib/goals.js`, stamped on the profile as
`profileVersion`. `storage.js` records the version it last published in
`il_publicProfileVersion` after every sync, and `Storage.republishProfileIfStale(userId)`
publishes once when that is behind. `App.jsx` calls it on sign-in after the cloud merge, so it
reads the merged log. One extra write per device per shape change; nothing on every open, so
*Updated Nd ago* on a friend's page still means their last log, not their last app open.

### Fourth pass — the board is live while it is open

Ben: *"I wanna be sure linked friends data remains current / live."* It was not. `FriendsScreen`
is always mounted (it returns null when closed), so `useFriends` fetched every friend's profile
once at app start and every opening of the sheet reused that until the refresh button. A phone
that keeps the PWA in memory for days showed a days-old board. My own row was always current —
`myProfile` is built from local data on render — which made the staleness easy to miss.

**Built.**
- `Storage.watchFriendProfiles(uids, onChange)` — one `onSnapshot` per friend's
  `users/{uid}/public/profile`; emits the full list only once every profile has reported, so
  the board never briefly shrinks; returns an unsubscribe.
- `useFriends(userId, open)` — holds `uids` separately from `friends`. Reads the friend list on
  mount and again on every open. While open, watches the profiles; a new `uids` array (add,
  remove, refresh) re-subscribes; close drops the listeners, so nothing is watched in the
  background. `addFriend` checks `uids` for duplicates instead of the last snapshot.
- `FriendsScreen` passes `open`.

**What "live" means now.** Their phone syncs 300 ms after a log (`setDataAndSync`), which
rewrites their profile; the listener on your open sheet delivers it in about a second. Closed,
nothing is fetched; the next open reads fresh. Reads: one per friend per open plus one per
change while open — nothing against the Spark quota at this scale. `getFriendProfile` stays
for the one-shot path.

**678 tests, 0 lint errors, build clean.** Not seen running — it needs two signed-in accounts.

---

## BTL-B41 / BTL-B42 — venue chips from where the phone is

**Merged 2026-09-18 on Ben's word — *"Push to prod please."* Not seen on a phone first; BTL-B46 still applies to the live site.**

Yesterday's ask, decided and built today. Ben, on the options: *"B42. Think on how it might work.
What data could it use? Ie choosing Redpoint Bristol or flashpoint based on GPS data?"* — then
*"What do you recommend?"* — then *"Build it."* The recommendation was the saved-venues list, on
its own, and that is what went to the branch.

**Why not a places source.** Redpoint and Flashpoint are a couple of kilometres apart and every
Bristol wall is hundreds of metres from the next; indoor Wi-Fi positioning is good to well under
100 m, so accuracy was never the problem — having a list of venues with coordinates was. A list
the app grows itself needs no Worker, no key, no rate limit and no third party in the privacy
copy, and after one visit to each wall the typing is gone. OpenStreetMap can sit behind the same
chips later for the first visit to a new wall, without changing anything built today.

**`src/lib/venues.js`** — pure, 37 tests. `recordVenue` upserts a typed name into
`profile.venues` (case-insensitive, whitespace collapsed, first spelling kept), moves its
coordinates to the latest fix, bumps `uses` and `lastUsed`, most-recent first, capped at 100.
`nearbyVenues` is the saved venues within 300 m of a position, nearest first, haversine.
`suggestVenue` returns the one venue in range, or nothing when two are — a guess wrong half the
time is worse than a chip.

**`src/hooks/useGeolocation.js`** — one fix on `locate()`, never on mount. `enableHighAccuracy`
off (indoors there is no GPS to wait for), a fix up to five minutes old accepted. Denied, no fix,
timeout and no API are all just a `status`; the form never depends on it.

**`src/hooks/useVenues.js`** — the list, in the profile so it syncs like everything else.

**`src/components/log/VenuePicker.jsx`** — the location input as it was, a pin button beside it,
chips underneath with the distance on each, one line of grey text when there are none and a
reason. Seen at 390 px in all six states (idle, locating, one venue prefilled, two venues, nothing
near, denied) in a throwaway harness under the dev server; a chip tap fills the field.

**`ClimbLogger.jsx`** — the first fix is always from a tap on the pin, so the permission prompt
comes from something Ben did. Once any venue has coordinates the logger asks for a fix on open, so
the chips are there before the field is reached — not when editing, where the phone's position
says nothing about a past session. The one venue in range prefills an *empty* field only; a name
already there is Ben's. On save the venue is recorded, with coordinates only for a session dated
today from a live fix: a back-dated session was somewhere else, and an edit is from wherever Ben
is sitting.

**Privacy.** `buildPublicProfile` reads `profile.name` and nothing else, checked — no coordinates
reach the friend-visible document. A *Your location* paragraph is drafted into the privacy
explainer and a line into the legal policy's §2.2, for Ben under BTL-B31.

**Docs.** `betalog_data_model.md` has the `Venue` shape; `types.js` the typedef. Cache v35 → v36.

**705 tests, 0 lint errors, build clean.** BTL-B46 is the phone check: tap the pin at a wall,
allow, save, and the venue should be offered without a tap next time the logger opens there.

---

## The level rides the grade — logger chips and History

Ben: *"Can we update the grade picker colours when you log it with the goal re the 'level'. We
also appear to have room in the history to display the level also."* The goal picker has coloured
its numbers by level band since 2026-09-13; the climb logger's chips were all still black, and the
History card said *Top: V4* in grey with nothing about what band that is.

**Merged 2026-09-18 on Ben's word — *"Push to prod please."* Not seen on a phone first; BTL-B47 is the check on the live site.**

**Built.**
- **`ClimbLogger.jsx`** — every unselected grade chip takes its band's colour from `gradeColor`
  (grey → teal → blue → purple → orange → red → amber up the ladder), exactly the goal picker's
  rule. The picked chip keeps the discipline accent, since that one means *selected*, and the
  level word appears under the row instead — *V7 · Expert* — the same line the goal picker
  draws. The *This session* list colours each logged grade the same way.
- **`SessionCard.jsx`** — a climb card carries a pill in the level's colours between the name
  and the feel: *V4 · ADVANCED*. It reads the hardest **send** in the session, not the hardest
  grade touched, because the explainer says the level word comes from what you own "not the
  hardest thing you have touched once"; a session with no send has no pill, and its *Top:* on
  the line below still names the hardest grade tried. Fixed on the way: *Top* was a string sort,
  which put V10 below V2 — a session with a V10 project and a V5 attempt read *Top: V5*. It now
  reads the ladder.
- **`SessionDetailSheet.jsx`** — each climb's grade in its band colour with the band word beside
  it, so the sheet and the card agree.
- **`lib/stats.js`** — `hardestGrade(climbs, sendsOnly)` is the one ladder-order reader, with
  `hardestSend` now a wrapper over it, and `climbGradeSystem(climb)` falls back from the
  discipline for a climb that predates the field. Both exported; 6 tests.
- Cache v36 → v37.

**Seen at 390 px** in a throwaway harness under the dev server with a stubbed data context:
the boulder and lead ladders, a picked V7 and 7a with their level lines, four History cards
(boulder with a V10 project reading *Top: V10* and *V4 · Advanced*, lead, a top-rope session with
only an attempt and so no pill, a gym session untouched) and the detail sheet. Harness removed.

**711 tests, 0 lint errors (13 warnings, all pre-existing), build clean.** BTL-B47 is the phone
check.
