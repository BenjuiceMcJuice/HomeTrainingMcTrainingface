# 2026-03-26 — Work Log

## What we did

### Step 6 — AI Coach
- Created `src/pages/Coach.jsx` — analyse-only (chat mode added then removed)
  - 4 personas (Jonas, Chad, Marina, Geoff) — switchable, persisted in localStorage
  - Groq API (llama-3.3-70b-versatile, temperature 0.7, 1400 max tokens)
  - Structured JSON analysis: summary, recovery/pyramid/plateau flags, focus area action cards
  - Persona voice injected at 3 levels (system prompt, analysis prompt per-field, user prompt)
  - Goals field with label + helper text, auto-saves on blur, feeds into AI context
  - Analyse button always at top, becomes "Re-analyse" after results
  - Better error handling (reads API error body, robust JSON parsing with cleanup)
  - 30-day session context with summary stats, rest gaps, per-session detail
- Added Coach as 5th nav tab (orange `#c0622a` accent, MessageCircle icon)
- Dashboard coach tip widget:
  - One-liner from selected persona, cached per day per persona
  - Refresh button to get a new tip (bypasses cache)
  - Visibility controlled by dashboard widget toggle system

### Bug fixes
- Schedule notification now shows all routine names when multiple scheduled on same day (was only showing first)
- Removed `autoFocus` from RoutineModal (name, description, exercise search) — stops iOS keyboard popping on open
- Exercise picker modal now full-height on mobile — keyboard no longer hides content

### Settings moved to app header
- Settings cog now in top header bar (always visible on any tab)
- Settings sheet: Name + Height on one row (compact), AI Coach toggle + Groq API key input, Restore defaults
- Hardcoded test key with fallback (only uses stored key if starts with `gsk_`)
- Removed settings from Plan tab

### Coach tab — analyse only
- Removed chat mode, kept structured analysis with persona voice
- Goals field with label + helper text, auto-saves on blur
- Analyse button at top, always visible
- Persona name injected into system + analysis prompt at 3 levels
- Better API error handling (reads error body, not just status)

### Climbing stats — split Boulder + Rope
- Grade tier mapping: V0-V17 and French 4-8b mapped to Beginner/Intermediate/Advanced/Expert/Elite/Pro/World Class
- Consistent grade = highest grade with ≥40% send rate and 3+ attempts
- Project/Consistent/Flash grade breakdown per discipline
- Separate level badges for Boulder and Rope
- Two dashboard widgets (Boulder level, Rope level) with Project/Consistent/Flash inline

### Dashboard widget system
- Max 4 toggleable widgets: Training load, Boulder level, Rope level, Coach tip, Weight & BMI
- Toggles in Profile tab as pill buttons with counter (e.g. "3/4 max")
- Quick stats strip compacted to single row (was 3 tall cards)
- All widgets respect toggle state via `profile.dashWidgets`
- Calendar defaults to expanded when no widgets active
- "Customise widgets in Plan → Profile" note at bottom of dashboard
- "Log more to get a level" shown when insufficient data for climbing level
- Goals moved from Profile to Coach tab (only relevant to AI)
- Profile tab is now first/default tab in Plan

### Polish
- Persona description shown below picker on Coach tab (e.g. "Jonas Ridge — Elite coach, calm, precise, data-driven")
- Name + Height on same row in Settings (smaller inputs)
- Coach tip dashboard widget has refresh button
- Coach tip cached per persona (switching persona = fresh tip)
- Rope level card shows project/flash even without consistent grade
- Removed old `il_coach_on_dash` localStorage flag (replaced by widget system)

### Climb session location
- Free text input on ClimbLogger save screen ("Where did you climb?")
- Stamped onto each Climb object + session.location on save
- Location persists between sessions (likely same venue)
- Shown in session detail sheet with pin icon
- Added to Session and Climb typedefs

### Data export/import
- Export JSON button in Settings — downloads all data (excludes API key)
- Import JSON button — file picker, validates, confirms replace, reloads
- Both in Settings under "Data" section

### PWA setup
- manifest.json with BL icon (black B, blue L, white background)
- Service worker (sw.js) — cache-first for app assets, network-first for API
- Apple meta tags for iOS home screen (standalone, theme colour)
- Service worker registered in main.jsx

### Firebase setup (Steps 1-4)
- Firebase project created: `betalog-340b3` (Spark/free plan)
- Google Authentication enabled
- Firestore database created (standard edition, europe-west2/London, production mode)
- Security rules set (users can only access their own data)
- Remaining: Step 5 (register web app + get config), then coding

### Code audit
- Fixed restore exercises/routines handlers — were calling `seedDefault*()` (only adds missing), now properly replaces all defaults while preserving user-created items + favourites
- Removed empty `TEST_KEY` variable and fallback pattern from App.jsx
- Simplified effectiveKey logic for Groq API key
- API key field changed back to `type="password"` (was `text` for debug)
- Confirmed no unused imports, no debug code, consistent ES5 `var` style, all null safety in place

## Files changed
`src/pages/Coach.jsx`, `src/pages/Dashboard.jsx`, `src/pages/Plan.jsx`, `src/App.jsx`,
`src/components/layout/Nav.jsx`, `src/components/routines/RoutineModal.jsx`,
`src/components/log/ClimbLogger.jsx`, `src/components/log/SessionDetailSheet.jsx`,
`src/components/profile/ProfileTab.jsx`, `src/components/profile/ClimbingStats.jsx` (new),
`src/hooks/useProfile.js`, `src/hooks/useWeightLog.js`,
`src/lib/types.js`, `src/main.jsx`,
`public/manifest.json` (new), `public/sw.js` (new), `public/icon.svg` (new),
`index.html`, `DEVLOG.md`, `logs/2026-03-26.md`
