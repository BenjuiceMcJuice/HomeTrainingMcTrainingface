# 2026-05-19 — Work Log

## Hangboard timer audio fixes

### Problem 1: sounds out of sync after backgrounding / pause
**Root cause:** Timer used `setInterval` tick-counting (one decrement per fire). Mobile browsers throttle intervals when backgrounded, causing the countdown to fall behind real time and sounds to fire late.

**Fix — wall-clock anchoring** (`HangboardTimer.jsx`):
- Added `phaseStartRef` — stores `{ at: Date.now(), timeLeft: n }` whenever a phase starts or resumes
- Interval now runs at 200ms and computes `remaining = timeLeft - Math.floor(elapsed / 1000)` from the wall clock, not tick counts
- If the app was backgrounded, the first interval fire after returning snaps the countdown to the correct remaining seconds
- Phase transition: sets `phaseStartRef = null` before calling `setTs(nextTimerState)` to prevent a 200ms re-entry race
- Same null-guard added to `skipPhase`, `togglePause` (on pause), and `endAndSave`

### Problem 2: sounds too quiet
Bumped all `playTone` gain values:
- `countdownTick`: 0.25 → 0.55
- `hangStart` / `readyStart`: 0.4 → 0.7
- `restStart`: 0.3 → 0.6
- `setRestStart`: 0.3 → 0.6
- `lastSeconds`: 0.2 → 0.55 (was the quietest)
- `done` chord: 0.3–0.35 → 0.6–0.7

### Bonus fix: `readyStart` was defined but never called
Added `sounds.readyStart()` to the phase-transition sound effect for `ts.phase === 'ready'`. Timer now beeps on "Get Ready" as well as on hang/rest transitions.

### Problem 3: beeps too harsh/thin
Rewrote `playTone` with a richer synthesis chain:
- **Two oscillators**: triangle wave (fundamental) + sine wave (octave above, at 25% level) — triangle has natural odd harmonics that sound warm/mellow vs a pure sine
- **Low-pass filter** (BiquadFilterNode) rolls off harsh upper frequencies
- **8ms linear attack** ramp on the master gain removes the click/pop from instant-on onset
- **Durations extended**: countdownTick/lastSeconds 70–80ms → 120ms so the timbre has time to register; done chord notes extended similarly

Overall character shifts from "electronic beep" towards soft chime/marimba.

**Files changed:** `betalog-react/src/components/log/HangboardTimer.jsx`

---

## Friends leaderboard

Replaced the slide-up `FriendsSheet` with a full-screen `FriendsScreen` component.

**Three views:**
- **Leaderboard** — ranked list (you + friends) by Boulder or Rope level; tap a row to see detail
- **Detail** — peak + 90-day current level cards for boulder/rope, last 3 sessions, remove friend
- **Manage** — friend code display/copy/generate, add-friend input

**Ranking:** `LEVEL_RANK` object maps the 7 levels to integers; tiebreaker by consistent grade index from `BOULDER_GRADES` / `ROPE_GRADES` arrays. "Me" entry computed via `buildPublicProfile(data.sessions, data.athleteProfile)`.

`App.jsx` updated: `FriendsSheet` → `FriendsScreen`, added `data={data}` prop.

**Files changed:** `betalog-react/src/components/friends/FriendsScreen.jsx` (new), `betalog-react/src/App.jsx`

---

## Firebase auth — login argument-error fix

**Problem:** After switching `getAuth` → `initializeAuth`, passing `browserPopupRedirectResolver` globally was removed — but `signInWithPopup` requires a resolver as its 3rd argument without it.

**Fix:** Export `browserPopupRedirectResolver` from `firebase.js`, import in `App.jsx`, pass explicitly to `signInWithPopup(auth, googleProvider, browserPopupRedirectResolver)`.

**Files changed:** `betalog-react/src/lib/firebase.js`, `betalog-react/src/App.jsx`

---

## Firebase auth — iOS sign-in hang fix (multiple iterations)

**Problem:** iOS sign-in hung indefinitely on "Signing in…".

**Root cause (popup):** iOS intercepts `window.open()` in new-tab context, opens Google auth in Safari. The popup promise never resolves back to the originating page.

**Attempted fix — redirect:** Switched to `signInWithRedirect` on mobile. New problem: Safari ITP clears cross-origin localStorage on navigation, so `getRedirectResult` throws `auth/missing-initial-state` on return. User ends up back on login page unauthenticated.

**Final fix — `visibilitychange` + 60s timeout:** Reverted to popup. Added `visibilitychange` listener so if the tab regains focus (user returned from Google in Safari), the loading state clears. Added 60s hard timeout as a catch-all. Popup-closed errors are silently swallowed.

**iOS PWA standalone fix (this session):** Detect `window.navigator.standalone === true` or `display-mode: standalone` before attempting Google sign-in. Show a message directing the user to sign in via Safari (same-origin localStorage means auth tokens carry over to the PWA automatically). Auto-expands email login form as the alternative.

**Files changed:** `betalog-react/src/App.jsx`
