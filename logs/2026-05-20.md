# 2026-05-20

## Friends leaderboard — peak primary, 90d secondary

**File:** `betalog-react/src/components/friends/FriendsSheet.jsx`

Redesigned `LevelRow` in the friend card:
- Peak grade is now the primary display (level badge + grade)
- 90d current grade shown as a small secondary indicator on the right, only if the user has actual 90d data
- Removed the old side-by-side peak/90d layout which was cluttered and confusing
- If no 90d data, the indicator is simply absent (streak already signals recency)
