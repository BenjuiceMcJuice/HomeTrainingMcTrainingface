# 2026-08-20

## Session start — todo review

Read `DEVLOG.md` and `logs/2026-08-19.md` to answer "what's on our todo list". Working tree clean,
no open PRs. Summary given: IA declutter (phases 0-3, all decisions settled, nothing blocking) is
the ready-to-build item; loose ends are the `step9-wip` rebase, the stray commit on the retired
`betalog-react` remote branch, the untested feedback round-trip, and the fact that `preprod` still
doesn't exist despite CLAUDE.md routing everything through it.

## Route A confirmed working on iOS

The one thing left unproven yesterday — whether iOS actually picks the calendar feed up — is now
proven. Ben subscribed on the phone and sent a screenshot of the event detail view.

What the screenshot confirms, beyond "it appeared":

- **The feed installed as its own calendar**, "BetaLog Training", not merged into an existing one.
  That is the `X-WR-CALNAME` we set being honoured.
- **`RRULE` expansion is correct.** iOS reads it back as "Repeats every week on Tuesday, Thursday
  and Saturday" — the `days[]` → `BYDAY` mapping survives the round trip through Apple's parser,
  which was the part most likely to be subtly wrong.
- **`DTSTART` anchoring works.** The event sits at 09:05-09:35 on the right day, so `remindFrom`
  is doing its job and the floating-time decision (no `TZID`) reads correctly on a real device.
- **The `URL` property surfaces** as a tappable betalog.co.uk row on the event.

Not visible in a static screenshot: whether the `VALARM` actually fired at 09:05. The alarm is
`TRIGGER;RELATED=START:PT0S`, so it should fire at the event start rather than before it.

`DEVLOG.md` updated — the "Still untested" note under **Calendar Worker deployed** is replaced with
the confirmation. Route A is end-to-end done.

## SDLC re-baselined to two stages

Ben asked to re-baseline the SDLC — "what it is and what it should be" — after the branch model kept
producing "deviations" in the logs.

### What it actually was

The documented `feature → preprod → main` flow **had never run once**. `preprod` points at
`add5889`, which is a commit in `main`'s own linear history dated 2026-08-10 — so the branch was
created by pointing at `main`'s tip and then never used. It never received a merge and was never
merged into `main`. `main`'s history is linear throughout, with no merge commits since 2026-07-12.

So yesterday's two "deviations from the branching model" weren't deviations. Direct-to-`main` is the
only mode this repo has ever had.

### What it is now

Two stages: **feature branch → `main`**. Rewrote `docs/guides/betalog_sdlc.md` and the Branches
section of `CLAUDE.md` to match.

The argument for dropping the middle stage, recorded in the guide so it doesn't get re-litigated:
Cloudflare Pages builds a preview URL for *every* branch, so verification on a real URL already
happens one stage earlier — `preprod` added no capability. A staging branch earns its place when
several people land work independently and a release needs a batching point. What actually protects
production here is the pre-merge checklist plus Cloudflare's instant rollback, and neither needs an
extra branch. Noted that if the third stage ever comes back it should come back with CI, or it will
drift the same way.

Also folded into the guide: `npm run lint` in the checklist, the Worker deploy step
(`workers/betalog-calendar/`), and the fact that Firestore rules do **not** ship with a merge to
`main` — a separate `firebase deploy` is required.

### Branch audit

24 remote branches. 15 `claude/*` are fully merged (0 ahead). Four carry unmerged commits — detailed
below. `betalog-react`'s two commits are already on `main` by content (`index.html` and `sw.js` are
byte-identical across the branches), so it is safe to delete. Nothing deleted yet.

### Unmerged work found — one item is a live security gap

`claude/skills-syntax-hZnz6` (2026-06-05, one commit) has been sitting unmerged for two and a half
months and contains security hardening that production still lacks:

- **`friendCodes` enumeration.** Live rules grant `allow read` on `friendCodes/{code}` to any
  authenticated user. `read` covers `list`, so any signed-in user can enumerate every friend code
  and the uid behind it. The branch narrows this to `allow get`, which permits the by-ID lookup the
  feature actually needs and blocks the query.
- **No security headers.** The branch adds `betalog-react/public/_headers` with CSP,
  `X-Frame-Options`, `X-Content-Type-Options` and `Referrer-Policy`. Production has no `_headers`
  file at all.
- Admin check moves from a hardcoded UID to a `centreAdmins/{uid}` lookup.

**Caveat that stops it being a straight merge:** the CSP was written in June, before the feedback
widget (2026-07-12), Cloudflare analytics (2026-08-10) and the calendar Worker (2026-08-19) existed.
As written, `script-src 'self'` and the `connect-src` list would block all three. The rules fix and
the CSP need separating — the rules change is good as-is, the CSP needs its allowlist rebuilt
against what the app loads today.

Other unmerged branches, all lower stakes:
- `claude/climbing-centre-rockgympro-review-f3o8f0` — 4 commits, ~1,300 lines of RGP integration
  strategy docs. Docs only, no app code.
- `claude/betalog-pixel-icons-z90bzh` — 2 commits, 78 files, a pixel-art icon set under `assets/`.
  Never wired into the app.
- `claude/daves-name-discrepancy-9rm63w` — 2 commits: a regional units toggle (miles default) across
  `CardioLogSheet`/`CardioStatsCard`/`SettingsSheet`, plus an identity & naming spec. Real product
  work, just never landed.

## Declutter phase 0 — invisible tidy

Both halves of phase 0 from `docs/specs/betalog_ia_declutter_spec.md`.

**Deleted `src/components/dashboard/GoalsWidget.jsx`** (60 lines). Confirmed imported nowhere before
removing, and `grep` is clean afterwards.

**Extracted BMI into `stats.js`.** `BMI_CATS`, `bmiCategory()` and a new `calcBMI(weightKg, heightCm)`
now live in one place. `WeightCard.jsx` and `ProfileTab.jsx` both import them; both previously
carried their own copy of the category table and the loop, in different dialects (`const`/arrow vs
`var`/`function`) but with identical behaviour. The formula itself was inline in both and is now
`calcBMI`.

`calcBMI` returns `null` rather than a number when either input is missing or non-positive, so
callers test the result instead of guarding the inputs — that is what both call sites were already
doing by hand.

**One deliberate behaviour change at an unreachable edge:** a negative weight or height used to
produce a negative BMI and get labelled "Underweight"; it now returns `null`. Not reachable through
the UI, and null is the honest answer.

### Verified

- `npm test` — **114/114** (107 before, plus 7 new in `src/lib/__tests__/bmi.test.js` covering the
  formula, the null cases, each band, and that boundaries are exclusive so 25.0 is Overweight).
- `npm run build` clean; `npm run lint` 0 errors, the same 16 pre-existing warnings.
- **Differential check** against both original inline implementations over every weight 0-200kg and
  height 0-220cm in 0.5 steps — 176,841 pairs, **0 mismatches** on both the BMI value and the
  category label. The refactor is behaviour-preserving.

Not driven in a browser. It is a pure extraction with unit coverage and a differential proof, and
the two call sites render the same values from the same inputs.

## Decisions

- **Pixel-art icon set ditched.** `claude/betalog-pixel-icons-z90bzh` (2 commits, 78 files) was never
  wired into the app; Ben called it. Deletion is pending — see below.
- **Branch deletions can't be done from a cloud session.** `git push --delete` returns HTTP 403 from
  the session's git proxy, which permits creating and updating refs but not deleting them. Tried
  three times with backoff and with both `--delete` and the `:refs/heads/...` refspec form. Recorded
  in DEVLOG as a laptop task.

## iOS alarm didn't fire — trigger rewritten to Apple's own form

Ben set a reminder for 09:05, then retested at ~12:30 with silent mode off. The event appears in the
Calendar app both times; the alarm fired neither time.

### Ruled out

- **Silent mode.** Present in the first test (bell-slash in the status bar), off in the second. Still
  no alert, so this was not it.
- **Refresh lag.** iOS picked up the moved event time almost immediately, so the feed is reaching the
  device promptly.
- **Malformed output.** Rendered the feed for a two-entry schedule and inspected every line with its
  octet count. No folding damage, no stray CRLF, `VALARM` correctly nested inside `VEVENT`, and all
  three properties `ACTION:DISPLAY` / `TRIGGER` / `DESCRIPTION` present — Apple silently drops alarms
  missing `DESCRIPTION`, so that was worth confirming.
- **Calendar settings.** "Event Alerts" is on for the subscribed calendar.

### The change

That leaves the trigger encoding. We were emitting:

```
TRIGGER;RELATED=START:PT0S
```

Valid RFC 5545, and the same instant Apple means by "at time of event" — but not how Apple writes it.
Apple's own exports use the negative zero-duration form with no explicit `RELATED`, since `START` is
the default:

```
TRIGGER:-PT0S
```

Changed to match, and added a stable per-entry `UID` inside the `VALARM` (Apple emits one; RFC 9074
sanctions it). Both are byte-level conformance changes — the alarm still fires at the chosen time,
so yesterday's decision against a `-PT10M` lead is untouched.

The offset is now a named constant `ALARM_TRIGGER` at the top of the alarm section, so the next step
on the ladder is a one-line change.

### If it still doesn't fire

The ladder, in order:

1. `-PT1M` — a small non-zero lead. If that fires and `-PT0S` didn't, the zero duration itself is
   what the parser rejects, and the fix is to pick the smallest lead that works.
2. If a non-zero lead doesn't fire either, this is an iOS limitation on alarms in *subscribed*
   calendars rather than anything in the feed, and no encoding change will fix it. **Route B (web
   push)** — deferred in `docs/specs/betalog_reminders_spec.md` — becomes the real answer, with the
   calendar feed staying as the visible-schedule half.

Worth being straight about the confidence here: step 1 of the ladder is a hypothesis, not a
diagnosis. The feed is provably well-formed, so the fault is in how iOS treats what we send, and
from this side that can only be narrowed by changing one thing and retesting on the device.

### Verified

`npm test` 114/114 (the `ics.test.js` alarm case now asserts the new form and that `RELATED=START` is
gone), `npm run build` clean, `npm run lint` 0 errors / 16 pre-existing warnings. Re-rendered the
feed and confirmed the emitted block.

**Needs a release to test** — the `.ics` is rendered client-side, so this has to reach `main` and be
refetched by iOS before the next attempt means anything.

## The real cause: iOS "Remove Alerts" strips every VALARM

The `-PT0S` change was released and the alarm still didn't fire. Ben then found it in
**Subscription Details → Remove Extra Items → Remove Alerts**, which iOS had set **ON**:

> Strip alerts or attachments from data added to the subscription calendar by the data provider.
> Default alerts will still apply according to your settings.

That is iOS deleting our `VALARM` as the feed is ingested. It explains every observation: the events
render perfectly, "Event Alerts" is on at the calendar level, a normal invite alerts fine, and no
alarm from the feed ever fires regardless of how the trigger is written.

**My trigger-encoding theory was wrong.** `TRIGGER;RELATED=START:PT0S` was never the problem; the
alarm was being discarded before anything parsed it. Worth recording *why* it was wrong rather than
just that it was: I reasoned from the feed outward — proved the `.ics` well-formed, ruled out silent
mode and refresh lag, and concluded the remaining variable must be how Apple parses what we send. The
variable I hadn't enumerated was iOS transforming the feed on ingest, which no amount of inspecting
our own output would ever have surfaced. The lesson is to enumerate the *consumer's* settings as
carefully as our own output.

### What was kept, and what was corrected

- **Kept `TRIGGER:-PT0S` and the VALARM `UID`.** Both are equivalent to what they replaced and match
  Apple's own exports, so there is no reason to churn them back. But the comments claiming iOS
  wouldn't fire the old form were a false causal claim and are now rewritten to say plainly that this
  is conformance, not a fix, and to point at the real cause.
- **Same correction in `ics.test.js`.**

### The product finding — this is the important part

**Every iOS user who subscribes hits this.** Nothing in the setup flow mentioned it, so the failure
mode for a new user is: the calendar appears, the sessions appear, and no reminder ever arrives. A
feature that silently does nothing is worse than one that visibly fails.

Added a highlighted note to the Settings calendar section, shown once the feed is enabled, next to
"Subscribe on this device" — states that iOS defaults **Remove Alerts** on, that the sessions will
still appear so the symptom is silent, and gives the exact tap path to fix it.

### Verified

`npm test` 114/114, `npm run build` clean, `npm run lint` 0 errors / 16 pre-existing warnings.
Not driven in a browser — the change is static copy inside an already-rendered section.

**Confirmed on device.** With Remove Alerts off, the alarm fired: a "Glutes!!! — BetaLog"
notification at 13:40, raised by the Calendar app from the subscribed feed. That closes the last
unproven step — Route A now works end to end, from setting a time in Plan through to a reminder on
the phone, and the `-PT0S` trigger form is confirmed working in the process.

The whole chain is now evidenced: app renders the `.ics` → Worker stores it in KV → iOS subscribes
and refetches → Calendar raises the alarm.

## friendCodes enumeration closed

`firestore.rules` granted `allow read` on `friendCodes/{code}` to any authenticated user. In
Firestore `read` is `get` + `list`, so any signed-in user could query the whole collection and walk
away with every friend code and the uid behind it. Narrowed to `allow get`.

**Checked the app first.** Both call sites in `storage.js` are `getDoc` on a known id — the collision
check when generating a code (`:540`) and `lookupFriendCode` (`:593`). There is no collection query
against `friendCodes` anywhere, so `get` alone is sufficient and nothing breaks.

### Proved it rather than reasoning about it

No firebase CLI in the session, but Java is present, so the Firestore emulator runs. Wrote
`betalog-react/scripts/check-firestore-rules.mjs` — 7 assertions covering the lookup that must keep
working, the enumeration that must not, the signed-out cases, and the write rules (re-checked so this
change couldn't have loosened them by accident). All 7 pass.

**Ran the control**: put `allow read` back and re-ran. The enumeration case flips to
`FAIL — Expected request to fail, but it succeeded`, everything else stays green. So the hole was
real and the test actually detects it — a rules test that passes against the broken rule would have
been worthless.

The script is committed but deliberately outside `npm test`: it needs Java and two packages that
aren't project dependencies. Documented in the SDLC guide beside the rules deploy step, with the
instruction to flip the rule and confirm a new case fails before trusting it.

### ⚠️ Not live yet

Rules do **not** ship with a merge to `main` — Cloudflare only deploys the app. Production stays
exposed until someone runs, from a machine with firebase auth:

```
cd betalog-react && firebase deploy --only firestore:rules
```

Flagged at the top of the DEVLOG open items.

## Declutter phase 1 — Plan tabs

Plan goes from `Profile | Exercises | Routines` to `Schedule | Routines | Exercises | Profile`,
ordered by how often each is opened.

### What moved

- **`ScheduleCard` out of `RoutinesTab`.** It sat above the routine list, so the most-used thing in
  Plan was something you scrolled past on the way to something else. It now has its own tab.
- **`CalendarReminders` out of the Settings sheet** and into `src/components/schedule/`. Extracted
  from `SettingsSheet.jsx` (93 lines) into its own component and restyled from a Settings-sheet block
  (`border-t` divider) to a Plan card (`bg-white rounded-2xl mx-4 border`) to match `ScheduleCard`.
  `SettingsSheet` lost the now-unused `CalendarPlus`/`Copy`/`Check` icons and the `useCalendarFeed`
  and `useSchedule` hooks along with it.
- **Default tab is now Schedule**, not Profile. Landing on the least-used tab was a leftover; the
  spec's ordering rationale implies the first tab should be the one you actually came for. Worth
  flagging as a behaviour change rather than a pure move.

This also fixes something introduced earlier today: the "Remove Alerts" warning shipped this morning
was sitting in Settings, three taps from the schedule it explains. It now sits directly under the
schedule, next to the Subscribe button it warns about. That is Rule 2 applied to actions — the thing
and its setup in one place — which is exactly the argument the spec made for this tab.

### Verified in a real browser

Driven in Chromium at 390×844 (temporary auth bypass in `App.jsx`, **reverted** — `grep` for
`BETALOG_TEST_BYPASS` is clean, and the throwaway driver script is deleted):

- Tab bar reads `["Schedule","Routines","Exercises","Profile"]` in that order, Schedule active on load.
- Schedule tab renders the Weekly schedule card and the Calendar reminders card with a working
  "Set up calendar reminders" button.
- Routines tab no longer contains a schedule section and starts at Training — asserted both ways
  (`/Weekly schedule/` false, `/Training/` true).
- The Settings sheet screenshot confirms the calendar section is gone: it now runs Name/Height → Data
  → Restore defaults → Feedback → Account.

Two things worth knowing for the next browser test: the app auto-opens the Settings sheet when the
profile has no name, which silently blocks clicks on the page beneath — close it first. And
`waitUntil: 'networkidle'` never settles, because Firestore retries forever without network; use
`domcontentloaded` and explicit waits.

`npm test` 114/114, `npm run build` clean, `npm run lint` 0 errors / 16 pre-existing warnings.

### Next

Phase 2 — the shared collapsible widget shell with state in `profile.widgetCollapsed`. It is the only
phase with genuinely new logic, and phase 3 (the dedup) depends on it.

### Follow-up: the move left stale pointers behind

Asked whether phase 1 was actually done, I re-audited rather than answering from memory — and it
wasn't. Moving a component is not finished when the code compiles; every reference to where it used
to live is also wrong. Six were:

- **`useCalendarFeed.js`** — the in-flight guard comment explained itself by saying "the root sync
  component and the Settings sheet both mount this hook". No longer true, and it is exactly the kind
  of comment someone trusts when debugging a duplicate-upload bug.
- **`calendarFeed.js`** — "revocable from Settings" in the header explaining the token's security
  model.
- **`CalendarFeedSync.jsx`** — justified mounting at the root by contrasting it with Settings.
  Rewritten to contrast with *any one screen*, which is the argument that actually generalises and
  now covers the Schedule tab too.
- **`docs/specs/betalog_reminders_spec.md`** — the component table still pointed at
  `src/components/layout/SettingsSheet.jsx`, a file path that no longer contains it. Plus two prose
  references.
- **`CLAUDE.md`** — the file-structure tree had no `schedule/` entry, and described `Plan.jsx` as
  "Exercises, routines, schedule, profile, climbing stats" rather than by its tabs.
- **`DEVLOG.md`** — the Route A summary told a reader to tap "Set up calendar reminders" in Settings.
  That is the canonical how-it-works description, so it would have sent someone to the wrong screen.

Deliberately left alone: the declutter spec's own "moved from Settings sheet" lines, which are
correct as a record of the plan, and the Route B sections describing a future push-notification UI in
Settings — that is undecided design for unbuilt work, not a stale fact, and quietly redesigning it
here would be the wrong call.

`npm test` 114/114, build and lint clean after the sweep.

## Declutter phase 2 — the collapsible widget shell

Built the shared shell and applied it. `npm test` 123/123 (114 + 9 new), build and lint clean —
still 16 warnings, none added.

### Shape

- **`src/lib/widgetCollapse.js`** *(new)* — `COLLAPSE_DEFAULTS`, `isCollapsed`, `toggleCollapsed`.
  Pure, so the resolution logic is unit tested and the component file stays a component file (putting
  them in `WidgetShell.jsx` added two `react-refresh/only-export-components` warnings; moving them
  out removed both).
- **`src/components/dashboard/WidgetShell.jsx`** *(new)* — header + chevron + conditional body.
- **`AlcoholFreeCard`, `CardioStatsCard`, `ActivityCalendar`** wrapped. `ActivityCalendar` loses its
  bespoke `defaultExpanded` prop and its local `expanded` state.
- **`Dashboard`** passes `editMode` into `renderWidget(key, editMode)`.

The shell sits *inside* each card's chrome rather than providing it — the cards have different
borders and padding, and unifying that is a visual change this phase deliberately doesn't make.

### The subtle bit, guarded by a test

`isCollapsed` tests own-property rather than truthiness. `map[key] || DEFAULT[key]` reads fine and is
wrong: once you expand a chart widget the stored `false` loses to the `true` default and it
re-collapses on every load. There is a test named for that regression.

### Two departures from the spec, both deliberate

**`GymStatsCard` is not on the shell.** Decision 2 lists `gymStats` as default-collapsed, but the
card is 52 lines and *entirely header* — a session count and a sets line, no chart, no detail.
Collapsed and expanded would render identically, so the chevron would be a control that does nothing.
Left alone. If it ever grows a body, it goes on the shell.

**`AlcoholFreeCard` now renders headline-first.** The chart used to sit *above* the alcohol-free
streak. Contract 1 says the header carries the headline and collapsing hides detail, so the streak
had to become the header and the chart the body — which reverses their order. Every other card is
already headline-then-detail, so this makes it consistent, but it is a visible change to a card you
didn't ask to have reordered.

### Verified in a real browser

Chromium via Playwright against the dev server, with a temporary auth bypass in `App.jsx` (reverted;
`grep` confirms zero references remain) and seeded sessions/drinks:

- Three collapse toggles render — `alcoholFree`, `cardioStats`, `activityCalendar`. `gymStats`,
  `trainingLoad`, `weight` and the level cards have none, as intended.
- **Defaults correct:** all three start collapsed (3 "Expand", 0 "Collapse").
- **Persistence works:** expanding cardio wrote `{"cardioStats":false}` to `il_athleteProfile`, and
  after a reload the state held (2 "Expand", 1 "Collapse").
- **Edit mode hides every toggle** — 0 collapse controls while dragging, so contract 4 holds.
- **Collapsed still says something** — screenshotted: "7 sessions", "2 sessions", "0 days
  alcohol-free", "75 kg · BMI 23.1" all readable with bodies folded.

### Noted for phase 3

The Dashboard footer reads "Customise widgets in Plan → Profile". Decision 4 moves the widget picker
into edit mode and renames that tab, so this line has to change with it.

## Deployment guide missed in the SDLC re-baseline

Looking up the preview URL for the phase 2 branch turned up two stale things in
`docs/guides/betalog_deployment.md`, which I hadn't touched when re-baselining the SDLC this morning:

- The URL table still listed `preprod.betalog.pages.dev` as "check releases here before promoting to
  `main`" — a branch retired hours earlier. Removed.
- The preview-deploy note was written around `preprod` being the thing you check. Rewritten around
  the feature branch preview being the release gate, which is what the two-stage flow makes it.

Also recorded a limit worth knowing: **Cloudflare truncates branch aliases at 28 characters**, so
cloud-session branch names often have no working `<branch>.betalog.pages.dev` URL and you need the
immutable per-commit URL from the Deployments tab. `claude-todo-list-review-fh7l55` is 30 characters,
so this bit immediately.

Same class of miss as the Schedule tab references: changing a decision means chasing every document
that encoded the old one, and grepping only the files I expected to be involved wasn't enough.

## Google sign-in doesn't work on preview deploys

Ben went to check phase 2 on the branch preview and couldn't log in with Google.

Not a bug in the branch. `LoginScreen.jsx:68` signs in with `signInWithPopup`, and Firebase only
permits OAuth on domains in its authorized list (Console → Authentication → Settings → Authorized
domains). That list holds `betalog.co.uk` and `betalog.pages.dev`. A branch subdomain such as
`claude-todo-list-review-fh7l55.betalog.pages.dev` is not on it, and **Firebase does not accept
wildcard domains**, so every preview fails with `auth/unauthorized-domain`.

**Workaround: email login**, which the screen already offers. `signInWithEmailAndPassword` does not
use the OAuth redirect flow, so it is unaffected by the authorized-domain list and works on any
origin. The app's own iOS error copy already says as much — "or use email login below".

### Why this matters beyond today

This is a hole in the release gate I wrote this morning. The two-stage flow leans entirely on
"verify on the branch preview" as the thing that protects production, and for anything behind Google
auth that gate silently doesn't work on a fresh branch — which is every cloud session. Documented in
the SDLC guide next to the verify step rather than left to be rediscovered, along with the 28-char
branch-alias truncation found in the same sitting.

Adding each preview subdomain to Firebase would fix it properly but is per-branch busywork. Not
worth it while email login exists; worth revisiting if the preview ever needs to be handed to someone
who only has a Google account.

## Blank draggable card in edit mode

Ben spotted an empty widget in edit mode — a drag handle with nothing beside it.

**Pre-existing, not from phase 2.** `git show 98c4457 -- Dashboard.jsx` confirms phase 2 touched only
`renderWidget`'s signature and the `ActivityCalendar` props; `SortableWidget` and `visibleKeys` are
untouched. The cause: several widgets return `null` when they have no data to show —
`GymStatsCard:12` (no gym sessions), `CardioStatsCard:75` (no 90-day history), `LevelCard:15` (no
graded climbs), `CoachTip:44` (no Groq key), `WeightCard:6` (no weight). `visibleKeys` filters on the
user's `dashWidgets` preference only, so an enabled-but-empty widget still got sortable chrome
wrapped around nothing.

I had seen this in my own phase 2 screenshot and dismissed it as level cards with no seeded data.
That was wrong, and it is the second time today that waving something through cost a round trip.

### Fix: measure, don't predict

`SortableWidget` now wraps children in a ref'd div and hides itself when that div has no element
children.

Rejected the alternative — a `hasContent(key)` predicate in `Dashboard` built from the data it
already holds — for two reasons. It would restate five components' null conditions in a sixth place,
exactly the duplication the declutter exists to remove; and it cannot work for `CoachTip`, which
returns `null` until an async fetch resolves, so no data check available at render time could predict
it.

Used a `MutationObserver` on `childList` rather than a dep-less `useLayoutEffect`. The dep-less
version worked but lint rightly flagged `setState` in an effect with no dependency list as a route to
an update loop; the observer takes `[]` deps, silences the warning legitimately rather than by
suppression, and still catches `CoachTip` filling in later.

### Verified

In Chromium, seeded with cardio-only sessions and no Groq key, no gym sessions and no weight log, so
five of the eight enabled widgets render nothing:

- **With the fix:** 8 drag handles in the DOM, **3 visible** sortable widgets.
- **With the fix reverted:** the same 8 handles, **8 visible**.

Worth recording honestly: the check also reported "0 blank visible widgets" in *both* runs, so that
particular assertion was worthless as a control — it looks for an empty content div, which only
exists in the fixed structure. The 8 → 3 visible count is the real evidence. A check that passes
against the broken code proves nothing, which is the same trap the Firestore rules control was
written to avoid.

`npm test` 123/123, build clean, lint 16 warnings — unchanged.

## The blank card was the AI coach — and it fails silently

Ben identified the blank widget: it is `CoachTip`, which he only ever sees flash briefly when
toggling edit/done.

**My hiding fix was not the cause, and it was right to notice it.** `CoachTip:38` had
`.catch(() => { /* silently fail */ })` followed by `.finally(() => setLoading(false))`. On a failed
Groq call that leaves `tip = null, loading = false`, which hits `if (!apiKey || (!tip && !loading))
return null` — the widget removes itself with no trace. The flash is exactly the `loading` state:
"Thinking…" renders, so there is content and the card is visible; the call fails; it returns null and
vanishes. The blank draggable box in edit mode was this widget's empty shell.

So the ordering was: coach broken (silently, for who knows how long) → phase 2 unrelated → my
SortableWidget fix hid the shell, which removed the last visible symptom. Hiding an empty widget is
still correct, but it would have buried this permanently. Worth remembering: a fix that makes a
symptom tidier can also make a fault invisible.

**Fix: stop swallowing.** `CoachTip` now tracks `failed`, logs the real error to the console, and
renders "Couldn't reach the coach — check your Groq key in Settings, or retry" beside the existing
retry button instead of disappearing. This surfaces the failure; it does not fix whatever is failing,
which needs the console output or the key checking — most likely an invalid/expired Groq key.

## Widgets changing shape while dragging

`Dashboard.jsx` styled the dragged element with `CSS.Transform.toString(transform)`. dnd-kit's
`Transform` helper includes `scaleX`/`scaleY`, and with `verticalListSortingStrategy` those scale
values come from the neighbouring items — so with cards as different in height as the alcohol card
and the gym card, a dragged widget visibly stretches or squashes toward whatever it is passing.

`CSS.Translate.toString(transform)` emits only the translation. Standard fix for this exact symptom.

`npm test` 123/123, build clean, lint 16 warnings — unchanged.

## The coach was broken by a model decommission, not the key

Ben found the actual cause: a Groq email saying **`llama-3.3-70b-versatile` was decommissioned on
16 August 2026** — four days ago. `Coach.jsx` hardcoded exactly that id, so every call has been
failing since, and `.catch(() => {})` meant nobody found out.

I had told him the model looked current. That was wrong and it sent him looking at his API key.

### Changes

- **`GROQ_MODEL` = `openai/gpt-oss-120b`**, Groq's recommended production replacement. The email also
  offers Qwen3.6 27B, but Groq lists that one as *preview* — evaluation, not production — so it is
  the wrong choice for a feature users depend on.
- **Model and endpoint pulled into named constants** at the top of `callGroq`, with a comment saying
  what happened and pointing at `console.groq.com/docs/deprecations`. It was a bare string in the
  middle of a fetch body; the next decommission should be a one-line change in an obvious place.
- **The tip card now shows Groq's own error text** — "Coach unavailable — {message}". `callGroq`
  already extracted `err.error.message` from the API response and threw it, so the API has been
  saying precisely what was wrong the whole time and we were discarding it. Had the generic message I
  shipped an hour earlier been the real one, this would have read "model has been decommissioned" and
  the diagnosis would have taken seconds instead of a detour through the API key.

### The lesson worth keeping

Three layers hid a clear upstream error: `.catch(() => {})` swallowed it, `return null` removed the
evidence, and my own fix tidied away the last visible symptom. Each was locally reasonable. Together
they turned "Groq told us exactly what was wrong" into a silent absence.

### Not verified against the live API

`npm test` 123/123, build clean, lint unchanged — but no call has been made to Groq with the new
model from here; that needs Ben's key. If `openai/gpt-oss-120b` is wrong for his account the card
will now say so in its own words, which is the point.

## Declutter phase 3a — the dedup

The half of phase 3 that removes duplication. Profile → Goals follows separately so it can be
reverted on its own.

- **`GradeChart` + `Legend` extracted** from `ClimbingStats.jsx` into
  `src/components/dashboard/GradeChart.jsx`.
- **Folded into `LevelCard`'s collapsible body**, which only became possible once phase 2 existed.
  The headline — level, project/consistent/flash, goal progress — stays in the shell header.
- **`ClimbingStats.jsx` deleted**, not just unmounted. It had one importer (`ProfileTab`) and nothing
  else; leaving it would have recreated exactly the dead-file problem phase 0 cleaned up.
- **Weight/BMI readout removed from `ProfileTab`** — `WeightCard` owns it, and phase 0 already made
  them share the same maths.
- **The code that only fed those blocks went too**: `calcWeightTrend`, a local `daysAgo`, the
  weight/BMI derivations, the `useWeightLog` call and four now-unused icon imports. `ProfileTab` is
  **214 → 117 lines**.

**Defaults: the level charts arrive collapsed.** Decision 2 listed `boulderLevel`/`ropeLevel` as
expanded, but that was written when their whole body was two lines of numbers. The spec's own
load-bearing warning wins: moving the grade charts onto the Dashboard only declutters if they arrive
folded, otherwise the scroll just moves house from Plan. Recorded in `widgetCollapse.js` next to the
values.

The phase 2 test caught this properly — `folds only the chart-heavy widgets` failed the moment the
defaults changed, which is what it was for. Updated, plus a new case asserting the glanceable widgets
stay expanded.

### Verified

`npm test` **124/124**, build clean, lint **15** warnings — one *fewer* than before, since deleting
`ClimbingStats` removed a `react-refresh` warning.

In Chromium with seeded boulder sessions:
- Level card renders with its headline; **grade bars hidden while collapsed** (0 matches for the
  legend), **visible after expanding** (1).
- Plan → Profile shows no weight readout and no grade bars.

One imprecise check worth noting: my first pass reported "Profile still shows BMI: true". It was
matching the widget picker's option *label*, "Weight & BMI", not a readout — confirmed by screenshot.
A substring match over `body.innerText` is a blunt assertion; the screenshot is what settled it.

## Declutter phase 3b — Profile becomes Goals

Decision 4, and with it the whole declutter.

### The picker moved; name and height just went

- **Widget picker → `src/components/dashboard/WidgetPicker.jsx`**, rendered inside the Dashboard's
  "Edit layout" mode. It now sits with the drag-to-reorder it belongs beside: choosing widgets and
  ordering them are one job that used to be split across two screens.
- **Name and height: deleted, not moved.** Decision 4 said move them to Settings — but
  `SettingsSheet.jsx:96-97` has edited both since long before this spec was written. The Plan tab was
  only *displaying* them, which is Rule 1's read-only-readout-on-a-Plan-tab case. So the "move" was a
  one-line deletion and no new Settings UI. Spec amended to record that.
- **`ProfileTab.jsx` deleted.** Once the picker and the name row were gone it wrapped nothing but
  `<GoalsSection />`, so `Plan` renders that directly rather than keeping a pass-through component.
- **Tab renamed**: `Schedule | Routines | Exercises | Goals`. The tab key is local `useState`, not
  persisted, so no stored `'profile'` value needed migrating.
- **Dashboard footer** now reads "Choose and reorder widgets with Edit layout" instead of pointing at
  a tab that no longer exists.

### Verified in Chromium

- Picker hidden outside edit mode (0), visible inside it (1).
- Plan tabs read exactly `["Schedule","Routines","Exercises","Goals"]`.
- Goals tab contains no widget picker.
- Footer text updated, old "Plan → Profile" string gone.

`npm test` 124/124, build clean, lint **15** warnings — one fewer than this morning.

### Two false alarms from my own checks today

Both were blunt assertions, not real faults: a `body.innerText` substring match for "BMI" hit the
widget picker's *label* "Weight & BMI", and a `text=/Choose and reorder/` locator returned 0 for text
that was demonstrably on the page. In both cases a direct check settled it in seconds. Worth
remembering that a test which can't distinguish "absent" from "my selector is wrong" is barely a test
— the same lesson as the rules-check control earlier.
