# 2026-03-25 — Work Log

## What we did

Dashboard, profile, schedule, and polish pass across the React rewrite.

### Hangboard edit fix
- Created `HangboardEditSheet.jsx` — full grip editing (fingers/grip/edge/timing/weight), matching gym/climb edit pattern
- Wired into `SessionDetailSheet` — hangboard Edit button now active (was greyed out)

### Data model cleanup
- Updated `HangGrip` typedef to include `fingers`, `gripType`, `edgeSize` (were in code but missing from types)
- Removed redundant `createdAt`/`updatedAt` from `GymLogSheet` — hook handles it
- Renamed `betalog_data_schema.md` → `betalog_data_model.md`, updated all 7 referencing files
- Added session field matrix table, `date` vs `createdAt` vs `updatedAt` semantics
- Added `trackingType` to SessionExercise and RoutineExercise in schema doc
- Added `routineId`/`routineName` to Session in schema doc
- Added `defaultWeight` to Exercise, `targetDuration`/`targetRest` to RoutineExercise

### Step 5 — Dashboard
- **Quick stats strip** — weekly streak (with best record), sessions this week, total
- **Training load card** — acute vs chronic workload ratio (ACWR-inspired), zone labels (Easing off / Sweet spot / Pushing hard), contextual explainer text, trend arrow + %
- **Activity calendar** — monthly grid with prev/next nav, colour-coded cells (gym=green, climb=orange, hang=purple, multi=neutral), today dashed border, collapsible (default collapsed), scheduled days shown in grey
- **Weight/BMI card** — conditional on profile toggle, shows current weight + BMI badge + 30d trend
- **Schedule notice** — "Due today: X" or "Next: X · Wednesday (2 days)"
- Removed "Dashboard" page heading

### Step 5b — Profile & Weight Log
- `useProfile` hook — read + merge-save
- `useWeightLog` hook — CRUD, sorted newest-first, null-safe
- Profile tab in Plan: name (readonly, set via settings), weight input that upserts today's weight log entry, BMI auto-calc, weight trend, goals, "show on dashboard" toggle
- Weight entries appear inline in History feed — small scale-icon rows, inline editable (date + kg), deletable
- Save button greyed out until changes detected

### Schedule
- Evolved `Schedule` type from singleton to `ScheduleEntry[]` (max 3)
- `useSchedule` hook with auto-migration from old format
- `ScheduleCard` component — routine picker + Mon–Sun day toggles, per-entry inline editing
- Moved from Profile to Routines tab (better fit)
- Schedule days shown as grey cells in dashboard calendar
- Dashboard notification for due/upcoming routines

### Settings cog
- Added to Plan tab header (gear icon)
- Opens slide-up sheet with Name + Height (rarely changed values)
- Name shown readonly on Profile tab

### Nav & visual polish
- Per-tab accent colours: Dashboard=blue, Log=green, History=orange, Plan=purple
- Removed page headings from History and Plan
- Tinted backgrounds tried and reverted (user preference: white)

## Files changed
`betalog_data_model.md`, `DEVLOG.md`, `src/lib/types.js`, `src/lib/storage.js`,
`src/pages/Dashboard.jsx`, `src/pages/History.jsx`, `src/pages/Plan.jsx`, `src/pages/Log.jsx`,
`src/App.jsx`, `src/components/layout/Nav.jsx`,
`src/components/log/HangboardEditSheet.jsx` (new), `src/components/log/HangboardTimer.jsx`,
`src/components/log/SessionDetailSheet.jsx`, `src/components/log/GymLogSheet.jsx`,
`src/components/profile/ProfileTab.jsx` (new), `src/components/routines/ScheduleCard.jsx` (new),
`src/hooks/useProfile.js` (new), `src/hooks/useWeightLog.js` (new), `src/hooks/useSchedule.js` (new),
`data_migration.md`, `step3_gym_session_logging.md`, `logs/2026-03-23.md`, `logs/2026-03-25.md` (new)

## End-of-day polish
- Compacted Profile tab: weight/BMI/trend merged into one widget card, toggle + save on same row
- Moved restore-defaults (exercises + hang routines) from tabs into Settings sheet — tap-to-arm confirm pattern
- Moved Schedule section to top of Routines tab (most-checked item first)
- Removed tab hint text from Routines tab
- Per-tab nav accent colours: Dashboard=blue, Log=green, History=orange, Plan=purple
- Removed page headings from Dashboard, History, Plan
