# 2026-09-04

Short session. One fix to the Route B branch, then handing over to a laptop session for the parts a
cloud container cannot do.

## The CORS allowlist never covered preview deploys

Found while writing the deploy runbook, not by anything failing — which is the only reason it was
found before Ben hit it.

`ALLOWED_ORIGINS` in both Workers listed `https://betalog.pages.dev` but **not** the per-branch
preview deploys Cloudflare actually builds, which come out as `<branch-slug>.betalog.pages.dev`. The
subscribe call is a `PUT` with a JSON content type, so it is a non-simple request: the browser sends
a preflight `OPTIONS` first, the Worker answers without an `Access-Control-Allow-Origin` header
because the origin is not in the list, and the request never leaves. Turning notifications on from a
branch preview would have failed with nothing useful on screen.

This was inherited, not new — I copied the list from `betalog-calendar` when writing `betalog-push`,
so **both** Workers had the same gap. Route A has presumably only ever been enabled from production
or localhost, which is why it never showed up. Worth stating plainly: the whole point of a preview
deploy is checking a change on a real URL before releasing it, and for anything touching a Worker
that has not actually been possible.

### The fix

`workers/shared/cors.js` — one module, imported by both Workers, holding the exact origins, the
preview pattern and the `corsHeaders` builder. Both Workers now call it; neither keeps its own copy.
The duplication is what let them drift into being wrong the same way, so removing the second copy
matters as much as widening the first.

The preview pattern is anchored at both ends and its subdomain label cannot contain a dot:

    /^https:\/\/[a-z0-9][a-z0-9-]*\.betalog\.pages\.dev$/

That was chosen against the two shapes that would matter if it were sloppy — a lookalike someone
else could register (`evil-betalog.pages.dev`, no dot before `betalog`) and a suffix attack
(`betalog.pages.dev.evil.com`). Both are now covered by tests rather than by my reading of the
regex.

**Verified:** 12 new origin checks in `workers/betalog-push/scripts/smoke-test.mjs`, 30 checks in
that file in total, all passing. Both Workers bundle — `betalog-push` 19.00 KiB, `betalog-calendar`
3.10 KiB. App untouched: 246 tests, build green, lint 0 errors.

**Not merged.** Same reason as yesterday — and note the Worker change would not ship on a merge
anyway. Workers deploy only via `wrangler deploy`, never from a Cloudflare Pages build.

## Handed over to the laptop

Ben asked whether the deploy could be done from this session. It cannot: this is a Claude Code web
session in an ephemeral cloud container, and every remaining step needs credentials or hardware that
live on his machine — `wrangler login` is a browser OAuth round trip, `firebase deploy` needs the
Firebase CLI logged in as him, the Pages environment variable is a dashboard action, and the final
test is a phone.

Pasting a `CLOUDFLARE_API_TOKEN` into this container would technically work and was deliberately not
suggested: a long-lived deploy credential in a sandbox that gets reclaimed, to save opening a
terminal, is a bad trade.

The ordered instructions are in `DEVLOG.md` under the Route B entry, written to be followed from a
local session with no reference back to this conversation.

## The laptop session — rules deployed, Worker live

Picked up the handover above. Two of the four remaining steps are done.

**The friendCodes rules fix is live.** Worth recording how nearly it wasn't: the first
`firebase deploy --only firestore:rules` ran from a `main` checkout and reported
`already up to date, skipping upload` — a success message for deploying the *vulnerable*
rules, because `main` still carries `allow read`. The CLI reads the working tree and knows
nothing about branches. Re-running from this branch uploaded properly (`released rules`).

**Production and `main` have now drifted, in the safe direction.** Live Firestore enforces
`allow get`; `main` still says `allow read`. The next rules deploy from a stale checkout
silently re-opens enumeration. Resolves on merge; flagged to Ben, decision his.

**`betalog-push` is deployed** — `https://betalog-push.benjuicemcjuice.workers.dev`, cron
`*/5` registered, 19.00 KiB, KV `SUBS` = `f4cfc67a81f34f3bae6cc825f519b6b8` (committed,
same reasoning as the calendar Worker's `FEEDS`).

Deployed **before** setting the secrets, against the order in the Worker's README. On a
first-ever deploy the script does not exist yet, so `wrangler secret put` stops to ask
whether to create it — and piping the key in, which is how you avoid echoing a secret,
feeds the key to that prompt instead. Deploying first removes the prompt. The Worker is
inert in the meantime: the cron fires, finds an empty KV and sends nothing.

**Verified against the live deployment:** preflight from `https://betalog.co.uk` returns
`204` with the origin reflected, and `https://evil-betalog.pages.dev` gets no
`Access-Control-Allow-Origin` at all. That is the first confirmation of the CORS fix
against a real Worker rather than the smoke test's stub. Locally: 246 app tests, 30 Worker
smoke checks, both green on Windows.

**The VAPID private key was pasted into the assistant transcript** in a screenshot, rather
than only the public half. Impact is bounded — a send needs the key *and* a subscription
endpoint, and the Worker has no `GET`, so KV cannot be read back out — but regenerating is
free until the first device subscribes, so that is the recommendation while the window is
open.

**Still outstanding:** the VAPID secrets, `VITE_VAPID_PUBLIC_KEY` as a Pages environment
variable on both preview and production, a rebuild, and the phone test. Nothing has sent a
notification yet.
