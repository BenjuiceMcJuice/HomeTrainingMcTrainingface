# 2026-05-27

## Contact developer button in settings

**`src/App.jsx`**
- Added "Contact the developer" mailto link in SettingsSheet under a new "Feedback" section (above Account). Opens `benjuice.apps@gmail.com` with subject "BetaLog feedback".

---

## Doc audit — full cleanup

**`DEVLOG.md`**
- Added three missing milestone rows: Firebase auth iOS fixes (2026-05-19), Friends leaderboard (2026-05-19), Friends cards redesign (2026-05-20).

**`docs/specs/betalog_data_model.md`**
- Added `"cardio"` to `Session.type` union.
- Documented six new cardio fields: `cardioActivity`, `cardioLabel`, `cardioDurationMins`, `cardioQuantity`, `cardioUnit`, `cardioPoolLength`.
- Updated session field matrix with cardio column.
- Added changelog entry dated 2026-05-27.
- Updated "Last updated" to 2026-05-27.

**`docs/strategy/betalog_partner_overview.md`**
- "Account and cross-device sync" updated from 🔧 In development → ✅ Live now.
- Added "Friends, leaderboard, and grade comparison — ✅ Live now".
- "Session logging" updated to include cardio.
- "Last updated" updated to May 2026.

**`docs/specs/betalog_default_routines.md`**
- Added "Inter-grip rest phase" section documenting the `grip-rest` timer phase added 2026-05-16.
- Documented full timer state machine phase sequence.
- Updated "Last updated" to 2026-05-27.

**`docs/specs/betalog_privacy_spec.md`**
- Replaced all `[Your full name]` placeholders with "Steve Owen".
- Replaced all `[your email address]` placeholders with "benjuice.apps@gmail.com".
- Updated TODO note to reflect current operating status.

---

## Gym log: add/remove sets during logging

**`src/components/log/GymLogSheet.jsx`**
- Added `addSet(cardIdx)` and `removeSet(cardIdx)` functions — `addSet` copies the last set's reps/weight as the default for the new row; `removeSet` drops the last row (minimum 1 set enforced).
- `ExerciseCard` now accepts `onAddSet` / `onRemoveSet` props and renders `+ Set` / `− Set` buttons below the set rows. `− Set` is hidden when only 1 set remains.
- Works in both add mode (from routine or single exercise) and edit mode (editing an existing session).
