# 2026-09-26

## Privacy — delete account, and the copy rewritten against the code

Ben, on the privacy next steps: *"Build a delete button. Make it in the settings perhaps and
requiring lots of 'are you sure' type messages. Share links. I'll take your recommendation. And
yes re wrote the docs so it's accurate."* The recommendation was to drop share links from the
copy, build deletion as its own change, and rewrite the copy before any page is built.

### BTL-B32 — Settings › Account › Delete account…

A red *Delete account…* button under *Sign out* opens its own sheet with three steps:

1. **What goes** — the log (with its session count), goals/routines/schedule/health/venues, the
   friend profile and friends' lists, the calendar feed and this device's reminders, the sign-in,
   this device's copy. Says what it cannot reach (feedback, reminders from other devices). An
   *Export your data first* button reuses Settings' JSON export.
2. **Are you sure?** — no undo, no backup, signing out keeps everything; a tick box must be ticked.
3. **Last check** — type DELETE, then sign in again (password field for email accounts, Google
   popup otherwise). *Keep my account* is on every step.

The fresh sign-in comes **before** anything is deleted. Firebase refuses `deleteUser` for a stale
sign-in, and discovering that after the Firestore documents were gone would leave a live, empty
account. Order after that: revoke the calendar feed and this device's push subscription
(best-effort), take the uid off every friend's list, delete the public profile, the friend code,
the main document, then the auth user, then every `il_` localStorage key, then reload.

Google sign-in in an installed iOS app cannot open the popup — the same limit as sign-in — so the
sheet says to do it from Safari instead of offering a button that hangs.

Files: `lib/accountDeletion.js` (new — order of operations, reauth, pure helpers),
`components/layout/DeleteAccountSheet.jsx` (new), `lib/storage.js` (`deleteCloudData`,
`clearLocal`, `betalogKeys`), `components/layout/SettingsSheet.jsx` (button, export pulled into a
function), `firestore.rules` (owner may delete their own friend code — **needs deploying, BTL-B72**),
`lib/__tests__/accountDeletion.test.js` (8 tests), `public/help.html` (Settings list, *Your data*,
FAQ), `public/sw.js` (v45).

Verified: build, 752 tests, lint (no new warnings). The three steps rendered in Chromium at 390 px
from a throwaway harness page (removed) with a fake user, for both sign-in methods: Continue is
disabled until the tick, *Delete forever* until DELETE and a password. **Not verified: an actual
deletion** — the cloud session cannot sign in to Firebase. BTL-B73 is that check, with a spare
account on the branch preview.

### BTL-B31 — the privacy copy

`docs/specs/betalog_privacy_spec.md` rewritten. The four known errors fixed (share links gone,
deletion and export as built, friends listed), and three more found by checking each sentence:

- *"Use the app without an account"* — not possible; `App.jsx` shows the login screen without a
  user. Removed from the explainer, §2.1, §2.2 and the rights list.
- Feedback *"stored in Firebase"* — it goes to the shared Benjuicey feedback Worker.
- Nothing about the **calendar feed or push reminders**, which send routine names and times to two
  Cloudflare Workers, nor the **admin read access** in `firestore.rules`. Both added (§2.2, §2.5,
  third-party table, security section).

Groq's list now includes session notes and goals, which `buildContext` sends. The spec ends with a
table naming the code behind each claim, so the next review can re-check rather than re-derive.

Left for Ben (BTL-B71): what the feedback Worker stores, which is not in this repo.

### The controller name was never right

The draft named the wrong person as data controller — a name picked up from a borrowed laptop's
user account when the May placeholders were filled. Ben: it must be **Ben Phipps**. Changed
everywhere a wrong name appeared: the privacy spec (header, §1, §9), the backlog, DEVLOG, the
2026-05-29 user audit and the old daily logs.


**Released to `main` on Ben's word ("Merge"), the same day.** The friend-code rule still needs deploying (BTL-B72) and a real deletion is still unchecked (BTL-B73).

## BTL-B63–B66 — four live bugs, fixed and released

Ben: *"Yes fix B63-B66."* Live bugs, so fixed and merged without a further ask.

- **B63** — `CalendarReminders.jsx`: *Plan → Routines → Schedule* → *Plan → Schedule*.
- **B64** — a push reminder tap now opens the routine. Two paths, because the service worker
  must not call `client.navigate()` (it rejects on an installed iOS app — the reason it was
  removed in v4): with no window open it opens `/log?routine=<id>`, which `Log.jsx` now turns
  into the same `openRoutine` request the Dashboard chip makes (and replaces the URL so a reload
  does not reopen it); with a window open it posts `{type: 'betalog-open', url}` to it before
  focusing, and a `NotificationRouter` in `App.jsx` navigates. A routine that is neither gym nor
  hang just lands on Log. Guide's *Notifications* line updated.
- **B65** — `CardioStatsCard.jsx` read `cardioQuantity` raw (miles, lengths) against a km goal.
  It now uses `getCurrentValue` and `calcGoalProgress` from `lib/goals.js`, the same reading as
  Plan › Goals, which the goals tests already cover for miles and lengths.
- **B66** — `ActiveGoalCard` gets `confirming`; the X becomes a red *Tap again to delete* for the
  3 s window, like Settings' restore buttons.

Verified by running the real app in Chromium at 390 px with Firebase replaced by stubs (a
throwaway Vite config aliasing `firebase/*`; removed afterwards) and a seeded log: the widget read
9.7 km / 10.0 km · 97% where it read 6 km; the goal X showed the confirm and reverted after 3 s;
`/log?routine=` opened the gym log sheet and the hang timer; a posted `betalog-open` message on
an open app routed to the gym sheet. The real service worker's `postMessage` path on a phone is
not something a desktop browser can show — worth a tap on the next reminder. Build, 752 tests,
lint. Cache v46.

## BTL-B26 · B71 · B72 — the privacy page, from this laptop

Ben on the laptop: *"Can you do them in order please?"*, then *"Merge push to main please."* This
session started from a `main` two days stale and rebuilt deletion and the copy on a branch before
the merge conflicted eight ways and showed the cloud session had shipped both this morning. That
branch was deleted unmerged; nothing from it survives except the page shell.

**B71 was a Check for Ben only because the cloud session could not see the other repo.** The
Benjuicey worker is on this laptop. `createSubmission` in `worker/src/index.ts` stores app ID,
trigram, type, status, name, email (or empty), message, timestamp, and the developer's
closure/notes fields; `email.ts` sends a Resend confirmation to the address if given and a
notification to the developer. No URL, user agent or IP. Nothing deletes a record — `sweep.ts`
only auto-closes — so the policy's two-year figure is a promise kept by hand. §2.6 says all of it.

**B72 was already done**: the friend-code delete rule this laptop deployed at 12:30 is the same
rule, one line reordered. Firebase confirmed it released.

**B26**: `public/privacy.html`, the spec's copy word for word in the explainer's shell — sticky
nav, table of contents, seven plain-English sections, then the policy with a legal basis under
each purpose. Effective and last-reviewed both 2026-09-26. Linked from Settings › Account beside
*Delete account…*, the guide's *Your data* chapter and footer, and the sign-in footer ("By signing
in you agree to the privacy policy"). Cache bumped. Merged to `main` on Ben's word.
